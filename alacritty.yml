env:
  TERM: xterm-256color
shell:
  program: zsh
  args:
    - -l
    - -c
    - "tmux"
font:
  size: 12
  offset:
    x: 0    # For letter spacing (default: 0).
    y: 5    # For line spacing (default: 0).

  normal:
    family: Hack Nerd Font
